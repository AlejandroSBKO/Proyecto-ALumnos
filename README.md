# Proyecto Alumnos — API y Frontend minimal

Instrucciones rápidas para ejecutar el proyecto localmente:

Hacer click derecho en reqrequirements.txt, clcik en abrir terminal integrado y ejecutar el siguiente codigo:

```powershell
pip install -r requirements.txt
```

3. Ejecutar el archivo llamado:

```powershell
app.py
```

